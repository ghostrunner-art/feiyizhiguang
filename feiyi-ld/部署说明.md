# 非遗之光 - Ubuntu 24 部署指南

## 🚀 超简单3步部署

### 1. 上传项目到服务器
```bash
scp -r feiyi/ user@your-server:/home/user/
```

### 2. 一键部署
```bash
cd feiyi
chmod +x deploy.sh
./deploy.sh
```

### 3. 启动网站
```bash
chmod +x start.sh
./start.sh
```

### 4. 访问网站
```
http://您的服务器IP:5000
```

## 🔧 如果脚本不工作，手动部署

```bash
sudo apt update
sudo apt install -y python3 python3-pip python3-venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn
python init_data.py
gunicorn --bind 0.0.0.0:5000 app:app
```

## 📝 常用命令

**后台运行:**
```bash
nohup ./start.sh &
```

**停止服务:**
```bash
pkill -f gunicorn
```

**查看进程:**
```bash
ps aux | grep gunicorn
```

---

🎉 **就这么简单！您的网站跑起来了！**